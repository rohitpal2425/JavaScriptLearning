//Get user to  to input a number using prompt("Enter a number"). Check if it the number is multiple of 5 or not.
let num= prompt("Enter a number:");
if( num %5==0)
{
    console.log("The number is multiple of 5");
}
else
{
    consoole.log("The number is not multiple of 5");
}